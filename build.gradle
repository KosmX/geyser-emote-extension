plugins {
    id 'java'
}

group 'io.github.kosmx.geyserEmotes'
version '0.1-SNAPSHOT'

repositories {
    mavenCentral()
    maven{
        name = "bundabrg-repo"
        url = "https://repo.worldguard.com.au/repository/maven-public"
    }
}

dependencies {
    implementation "org.geysermc:connector:${project.geyser_plugin}"

}
